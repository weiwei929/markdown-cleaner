# Markdown Cleaner 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# 服务器端口（默认：3000）
PORT=3000

# 运行环境（development | production）
# 生产环境会隐藏详细错误信息
NODE_ENV=development

# 文件上传大小限制（字节，默认：5MB）
# 5MB = 5242880
# 10MB = 10485760
MAX_FILE_SIZE=5242880

# Gemini AI API Key（可选，用于 AI 专家版功能）
# 获取地址：https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# CORS 允许的来源（生产环境建议配置）
# 多个域名用逗号分隔，例如：https://example.com,https://www.example.com
# 开发环境留空允许所有来源
CORS_ORIGINS=

# API 限流配置
# 通用 API 限流：每个 IP 在 15 分钟内允许的最大请求数（默认：100）
RATE_LIMIT_MAX=100

# CPU 密集型接口限流：每个 IP 在 1 分钟内允许的最大请求数（默认：10）
# 影响接口：/api/process-text, /api/analyze, /api/apply-fixes, /api/plan, /api/preview-fixes, /api/ai/suggest
RATE_LIMIT_HEAVY_MAX=10